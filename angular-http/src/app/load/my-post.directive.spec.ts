import { MyPostDirective } from './my-post.directive';

describe('MyPostDirective', () => {
  it('should create an instance', () => {
    const directive = new MyPostDirective();
    expect(directive).toBeTruthy();
  });
});
